# Loaded at deploy time for Firebase Functions v2 params (defineString).
# Copy to .env and fill in values. Never commit .env (gitignored).

MAKE_WEBHOOK_URL=
KORAPAY_SECRET_KEY=
PAYSTACK_SECRET_KEY=
